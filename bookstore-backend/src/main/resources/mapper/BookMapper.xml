<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.BookMapper">

    <select id="selectAll" resultType="com.bookstore.pojo.Book">
        SELECT * FROM book
    </select>

    <select id="selectById" resultType="com.bookstore.pojo.Book">
        SELECT * FROM book WHERE book_id = #{bookId}
    </select>

    <select id="selectByCategory" resultType="com.bookstore.pojo.Book">
        SELECT * FROM book WHERE category_id = #{categoryId}
    </select>

    <select id="searchByKeyword" resultType="com.bookstore.pojo.Book">
        SELECT * FROM book WHERE book_name LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="bookId">
        INSERT INTO book (book_name, author, category_id, price, stock, cover_img, description, status)
        VALUES (#{bookName}, #{author}, #{categoryId}, #{price}, #{stock}, #{coverImg}, #{description}, #{status})
    </insert>

    <update id="update">
        UPDATE book SET book_name=#{bookName}, author=#{author}, category_id=#{categoryId},
        price=#{price}, stock=#{stock}, cover_img=#{coverImg}, description=#{description}, status=#{status}
        WHERE book_id=#{bookId}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE book_id = #{bookId}
    </delete>

</mapper>
